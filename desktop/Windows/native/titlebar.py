"""Сдвиг кнопок окна — на Windows делать нечего.

Модуль существует ради межплатформенного контракта: `shared/ui/main_window.py`
импортирует `native.titlebar.sink`, и проверка
`test_native_contract_covers_both_platforms` требует, чтобы каждое имя,
которое shared/ берёт из native/, существовало на **обеих** платформах. Без
этого файла проверка падает — она для того и написана, чтобы ловить такие
расхождения здесь, а не на машине пользователя.

Вызова всё равно не будет: единственное место, откуда `sink()` зовут,
закрыто проверкой `sys.platform == "darwin"`. Заглушка молчит намеренно —
светофора в заголовке Windows нет, двигать нечего, и сообщать об этом
некому.
"""
from __future__ import annotations


def sink(win_id: int, center_from_top: float, left: float) -> None:
    """Ничего не делает. См. docstring модуля."""
